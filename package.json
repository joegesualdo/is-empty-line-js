{
  "name": "@joegesualdo/is-empty-line",
  "repository": "joegesualdo/is-empty-line-js",
  "version": "0.0.1",
  "description": "Detect if a line is empty.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "npm run build && ./node_modules/ava/cli.js -v test.js",
    "build": "./node_modules/distify-cli/cli.js --input-file=./index.js --output-dir=./dist --is-node",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "distify-cli": "0.0.8"
  },
  "dependencies": {},
  "keyworkds": [
    "empty",
    "line",
    "string"
  ],
  "author": {}
}
